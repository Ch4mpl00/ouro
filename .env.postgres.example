# Postgres + pgvector container env. Copy to .env.postgres and fill in.
# Read by both the `postgres` service (to initialize the role/db on first
# boot) and by `mcp` (interpolated into DATABASE_URL).

POSTGRES_USER=mcp
POSTGRES_PASSWORD=change-me
POSTGRES_DB=mcp
